services:
  - type: web
    name: auther
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: node server/index.js
    healthCheckPath: "/api/health"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        value: admin@zamai.com
      - key: ADMIN_PASSWORD
        value: Admin2026!Secure
    autoDeploy: true
